apiVersion: v1
kind: Service
metadata:
  labels:
    app: openshift-basics-training
  name: openshift-basics-training
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: openshift-basics-training
  sessionAffinity: None
  type: ClusterIP
