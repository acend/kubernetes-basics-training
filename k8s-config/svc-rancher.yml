apiVersion: v1
kind: Service
metadata:
  labels:
    app: kubernetes-basics-training-rancher
  name: kubernetes-basics-training-rancher
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: kubernetes-basics-training-rancher
  sessionAffinity: None
  type: ClusterIP
